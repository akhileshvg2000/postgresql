***Batch Deletion***

10000 rows per commit, 1 second delay
Table locks for each 10000 rows until transaction commits.

dead lock

Index updation for each commit

WAL for each commit, replication lags.
